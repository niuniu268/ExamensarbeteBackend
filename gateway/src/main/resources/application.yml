server:
  port: 10010
logging:
  level:
    cn.itcast: debug
  pattern:
    dateformat: MM-dd HH:mm:ss:SSS
spring:
  application:
    name: gateway
  cloud:
    nacos:
      server-addr: nacos:8848 # nacos??
    gateway:
      routes:
#        - id: user-service # ?????????
#          uri: lb://userservice # ???????
#          predicates: # ???????????????
#            - Path=/user/** # ?????????????/user?????????
        - id: order-service
          uri: lb://orderservice
          predicates:
            - Path=/order/**
      default-filters:
        - AddRequestHeader=Truth,Itcast is freaking awesome!
